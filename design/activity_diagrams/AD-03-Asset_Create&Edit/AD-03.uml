@startuml
title AD-03: Asset Creation and Editing (Swimlane Diagram)

|User|
start
:Enter credentials;

|System|
:Receive login credentials;
:Validate credentials;

if () then ([valid])
    :Load dashboard;
    :Load asset table;
else ([invalid])
    :Display authentication error;
    stop
endif

' ------------------ ADD ASSET FLOW ------------------
|User|
:Select Add Asset;

|System|
:Load empty asset form;

|User|
:Enter asset details;

|System|
:Capture asset input;
:Validate required fields;

if () then ([complete])
    :Store asset data;
    :Refresh asset table;
else ([incomplete])
    :Display validation error;
    -> [return to form] :Load empty asset form;
endif

' ------------------ EDIT ASSET FLOW ------------------
|User|
:Select Edit Asset;

|System|
:Load existing asset data into form;

|User|
:Modify asset details;

|System|
:Capture updated data;
:Validate required fields;

if () then ([valid])
    :Update asset in database;
    :Refresh asset table;
else ([invalid])
    :Display validation error;
    -> [return to form] :Load existing asset data into form;
endif

stop
@enduml

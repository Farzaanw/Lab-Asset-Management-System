@startuml
title Swimlane Activity Diagram: Asset Searching and Filtering

|User|
start
:Enter credentials;
|System|
:Receive login credentials;
:Validate credentials;

if () then ([valid])
    :Load dashboard;
    :Load asset table;
    :Display search bar;
    :Display status filter options;
else ([invalid])
    |System|
    :Display authentication error;
    stop
endif

' --- LOOP FOR SEARCH & FILTER INPUT ---
repeat
    |User|
    :Provide input (keyword or status);
    |System|
    :Monitor input events;

    if () then ([keyword entered])
        :Capture keyword;
        :Filter assets by keyword;
        :Update asset table;
    else ([no keyword])
        if () then ([status selected])
            :Capture status value;
            :Filter assets by status;
            :Update asset table;
        else ([no status])
            :Display full asset list;
        endif
    endif

repeat while (new input received?)

stop
@enduml

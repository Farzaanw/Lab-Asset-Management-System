@startuml
title Dashboard Asset View and Listing (Swimlane Format)

|User|
start
:Enter username and password;
:Submit credentials;

|System|
if (Credentials valid?) then (Yes)
    :Verify account in system;
    :Identify user role
    (Research Student or Lab Manager);

    if (Role = Research Student?) then (Yes)
        :Load Research Student Dashboard;
    else (No)
        :Load Lab Manager Dashboard;
    endif

    |Dashboard|
    :Display initial asset list (limited set);

    |User|
    repeat
        :Scroll through dashboard;
        if (More assets needed?) then (Yes)
            |Dashboard|
            :Load additional assets;
        endif
    repeat while (User selects "List More Assets")

    |User|
    stop

else (No)
    |System|
    :Display invalid credentials error;
    |User|
    stop
endif

@enduml
